version: "3.9"
services:
  __container__-debug:
    build:
      context: .
      dockerfile: Dockerfile.debug
    image: ${LOCAL_REGISTRY}:5000/__container__-debug:${TAG}
    ports:
      - ${PORT}:2222
